server:
  port: 8081

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.40.128:8848
  application:
    name: nacos-consumer

management:
  endpoints:
    web:
      exposure:
        include: '*'